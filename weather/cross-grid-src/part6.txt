${stamp}。已檢查 ${places.length} 個地點、${dates.length} 個日期。`}
  $('projects').onchange=()=>{if(readonly)return;state.activeId=$('projects').value;save();render();update()};$('size').onchange=()=>{state.textSize=$('size').value;save();document.documentElement.dataset.size=state.textSize};$('refresh').onclick=()=>update(true);
  $('addCol').onclick=()=>{if(readonly)return;project().columns.push({id:uid(),locationId:initial[0].id});save();render();update()};$('addRow').onclick=()=>{if(readonly)return;const p=project();p.rows.push({id:uid(),date:core.shift(p.rows.at(-1).date,1)});save();render();update()};
  $('dateForm').onsubmit=e=>{e.preventDefault();if(readonly)return;const p=state.projects.find(p=>p.id===dateTarget?.projectId),row=p?.rows.find(r=>r.id===dateTarget.rowId);if(row){sequence(p,$('date').value);save();render();update()}$('dateDialog').close()};$('cancelDate').onclick=()=>$('dateDialog').close();
  function nameDialog(mode){if(readonly)return;nameMode=mode;$('nameTitle').textContent=mode==='new'?'建立專案':'更改名稱';$('name').value=mode==='new'?'':project().name;$('nameDialog').showModal();$('name').focus()}
  $('new').onclick=()=>nameDialog('new');$('rename').onclick=()=>nameDialog('rename');$('cancelName').onclick=()=>$('nameDialog').close();$('nameForm').onsubmit=e=>{e.preventDefault();if(readonly)return;const name=$('name').value.trim();if(!name)return;if(nameMode==='new'){const p=fresh(name);state.projects.push(p);state.activeId=p.id}else project().name=name;save();render();update();$('nameDialog').close()};
  $('search').onsubmit=async e=>{e.preventDefault();if(readonly)return;const query=$('query').value.trim();if(!query)return;const seq=++searchGeneration;searchController?.abort();searchController=new AbortController();$('searchStatus').textContent='搜尋中…';$('searchResults').replaceChildren();try{const data=await json('https://geocoding-api.open-meteo.com/v1/search?'+new URLSearchParams({name:query,count:10,language:'zh',format:'json'}),searchController.signal);if(seq!==searchGeneration)return;$('searchStatus').textContent=data.results?.length?'請選擇正確的城市與國家。':'找不到地點，請試英文名稱。';for(const r of data.results||[]){const b=el('button',r.name);b.type='button';b.append(el('small',[r.admin1,r.country].filter(Boolean).join('，')));b.onclick=()=>{const p=state.projects.find(p=>p.id===placeTarget?.projectId),col=p?.columns.find(c=>c.id===placeTarget.columnId);if(!col)return;let found=state.locations.find(l=>l.lat===r.latitude&&l.lon===r.longitude);if(!found){found={id:uid(),name:r.name+'（'+(r.country||r.admin1||'')+'）',english:'',lat:r.latitude,lon:r.longitude};state.locations.push(found)}col.locationId=found.id;save();render();update();$('placeDialog').close()};$('searchResults').append(b)}}catch(e){if(seq===searchGeneration)$('searchStatus').textContent='無法搜尋，請確認網路後重試。'}};
  $('placeDialog').addEventListener('close',()=>{searchGeneration++;searchController?.abort()});
  $('export').onclick=()=>{if(readonly)return;const p=project(),needed=new Set(p.columns.map(c=>c.locationId)),data={format:'weather-cross-grid',version:1,project:p,locations:state.locations.filter(l=>needed.has(l.id))};const url=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'})),a=el('a');a.href=url;a.download=p.name.replace(/[\\/:*?"<>|]/g,'_')+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)};
  $('import').onclick=()=>$('file').click();$('file').onchange=async()=>{if(readonly)return;try{const f=$('file').files[0];if(!f)return;const data=JSON.parse(await f.text());if(data.format!=='weather-cross-grid'||data.version!==1)throw Error();const p=data.project,ls=data.locations;if(!p||typeof p.name!=='string'||!Array.isArray(p.rows)||!p.rows.length||!Array.isArray(p.columns)||!p.columns.length||p.rows.length>366||p.columns.length>50||!Array.isArray(ls)||ls.some(l=>!Number.isFinite(l.lat)||Math.abs(l.lat)>90||!Number.isFinite(l.lon)||Math.abs(l.lon)>180||typeof l.name!=='string'))throw Error();if(p.rows.some(r=>typeof r.id!=='string'||!/^\d{4}-\d{2}-\d{2}$/.test(r.date)||!Number.isFinite(+new Date(r.date+'T00:00:00Z')))||p.columns.some(c=>typeof c.id!=='string'||!ls.some(l=>l.id===c.locationId)))throw Error();if(new Set(p.rows.map(r=>r.id)).size!==p.rows.length||new Set(p.columns.map(c=>c.id)).size!==p.columns.length)throw Error();const ids=new Map();ls.forEach(l=>{const n={...l,id:uid()};ids.set(l.id,n.id);state.locations.push(n)});p.columns=p.columns.map(c=>({...c,locationId:ids.get(c.locationId)}));p.colors=Object.fromEntries(Object.entries(p.colors||{}).filter(([,v])=>v==='blue'||v==='orange'||v==='hidden'));p.id=uid();p.name=p.name+'（匯入）';sequence(p);state.projects.push(p);state.activeId=p.id;save();render();update()}catch{$('status').textContent='無法匯入，請選擇這個天氣對照表匯出的專案檔案。'}finally{$('file').value=''}};
  $('editReport').onclick=()=>{if(readonly)return;$('reportText').value=project().reportText||'';$('reportDialog').showModal();$('reportText').focus()};$('cancelReport').onclick=()=>$('reportDialog').close();$('reportForm').onsubmit=e=>{e.preventDefault();if(readonly)return;project().reportText=$('reportText').value.trim();save();renderReport();$('reportDialog').close()};$('reportLink').onclick=()=>{const text=String(project().reportText||'').trim();if(!text)return;$('reportContent').textContent=text;$('reportViewDialog').showModal()};$('closeReportView').onclick=()=>$('reportViewDialog').close();
  $('print').onclick=()=>window.print();setInterval(()=>{if(!document.hidden)update(true)},1800000);document.addEventListener('visibilitychange',()=>{if(!document.hidden&&Date.now()-lastAttempt>=1800000)update(true)});
  if(readonly){document.documentElement.dataset.readonly='true';$('readonlyNotice').hidden=false;$('projects').disabled=true;}
  $('share').onclick=async()=>{
    if(readonly)return;
    const button=$('share');button.disabled=true;
    try{
      const p=project(),payload=compactShare(p),token=await compressShareText(JSON.stringify(payload)),base=location.href.split('#')[0],url=base+'#share='+token;
      let copied=false;
      try{await navigator.clipboard.writeText(url);copied=true}catch{}
      if(!copied){const area=document.createElement('textarea');area.value=url;area.style.position='fixed';area.style.opacity='0';document.body.append(area);area.select();copied=document.execCommand('copy');area.remove()}
      $('status').textContent=copied?'唯讀分享網址已複製。對方開啟後只會看到專案名稱、分析報告與天氣表格，不能修改。':'無法自動複製網址，請從瀏覽器網址列複製目前頁面網址後再分享。';
    }catch(e){$('status').textContent='無法產生分享網址，請重新整理後再試一次。'}finally{button.disabled=false}
  };
  save();render();update();
})();
</script></body></html>