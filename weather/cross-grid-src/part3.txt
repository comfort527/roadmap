,4)),md=target.slice(5),last=validDate(year-1,md),end=Math.min(year-1,Number(today.slice(0,4))-1),rain=[];if(last&&last<=cutoff)all.add(last);for(let y=end-9;y<=end;y++){const d=validDate(y,md);if(d&&d>= '1940-01-01'&&d<=cutoff){rain.push(d);all.add(d)}}items.set(target,{last,lastAvailable:!!last&&last<=cutoff,rain,startYear:end-9,endYear:end})}return{dates:[...all].sort(),items}}
 function calculate(item,daily){const index=new Map((daily.time||[]).map((d,i)=>[d,i])),i=item.lastAvailable?index.get(item.last):undefined;let wet=0,count=0;for(const date of item.rain){const j=index.get(date),v=j===undefined?null:daily.rain_sum?.[j];if(Number.isFinite(v)){count++;if(v>0.1)wet++}}return{kind:'reference',low:i===undefined?null:daily.temperature_2m_min?.[i],high:i===undefined?null:daily.temperature_2m_max?.[i],rise:i===undefined?null:daily.sunrise?.[i],set:i===undefined?null:daily.sunset?.[i],rain:count?100*wet/count:null,samples:count,wet,last:item.last,startYear:item.startYear,endYear:item.endYear}}
 const api={validDate,plan,calculate};if(typeof module!=='undefined')module.exports=api;else root.HistoryReference=api;
})(typeof window!=='undefined'?window:globalThis);
</script><script>(async() => {
  'use strict';
  const $=id=>document.getElementById(id),core=MatrixCore,KEY='weatherCrossGridV1',uid=()=>crypto.randomUUID();
  function bytesToB64u(bytes){let bin='';for(const b of bytes)bin+=String.fromCharCode(b);return btoa(bin).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')}
  function b64uToBytes(token){let s=token.replace(/-/g,'+').replace(/_/g,'/');while(s.length%4)s+='=';const bin=atob(s);return Uint8Array.from(bin,c=>c.charCodeAt(0))}
  function b64uEncode(text){return bytesToB64u(new TextEncoder().encode(text))}
  function b64uDecode(token){return new TextDecoder().decode(b64uToBytes(token))}
  async function compressShareText(text){if(typeof CompressionStream==='undefined')return 'j.'+b64uEncode(text);const cs=new CompressionStream('deflate-raw');const stream=new Blob([new TextEncoder().encode(text)]).stream().pipeThrough(cs);const bytes=new Uint8Array(await new Response(stream).arrayBuffer());return 'z.'+bytesToB64u(bytes)}
  async function decompressShareToken(token){if(token.startsWith('z.')){if(typeof DecompressionStream==='undefined')throw new Error('此瀏覽器不支援分享網址解壓縮');const ds=new DecompressionStream('deflate-raw');const stream=new Blob([b64uToBytes(token.slice(2))]).stream().pipeThrough(ds);return new TextDecoder().decode(await new Response(stream).arrayBuffer())}if(token.startsWith('j.'))return b64uDecode(token.slice(2));return b64uDecode(token)}
  function compactShare(p){const columns=p.columns.map(c=>{const l=loc(c.locationId);return[l?.name||'',l?.english||'',l?.lat??null,l?.lon??null]}),rows=p.rows.map(r=>r.date),cells=p.rows.map(r=>p.columns.map(c=>{const v=p.colors?.[cellKey(r,c)];return v==='blue'?'b':v==='orange'?'o':'h'}));return{v:1,n:p.name||'分享專案',r:String(p.reportText||''),d:rows,l:columns,c:cells}}
  function expandShare(data){if(!data||data.v!==1||!Array.isArray(data.d)||!Array.isArray(data.l)||!Array.isArray(data.c))throw new Error('分享資料格式錯誤');const locations=data.l.map((a,i)=>({id:i===0?'taipei':uid(),name:String(a?.[0]||''),english:String(a?.[1]||''),lat:Number(a?.[2]),lon:Number(a?.[3])}));if(!locations.length)throw new Error('分享資料沒有地點');const columns=locations.map(l=>({id:uid(),locationId:l.id})),rows=data.d.map(date=>({id:uid(),date:String(date)})),colors={};rows.forEach((r,ri)=>columns.forEach((c,ci)=>{const v=data.c?.[ri]?.[ci];colors[cellKey(r,c)]=v==='b'?'blue':v==='o'?'orange':'hidden'}));const project={id:uid(),name:String(data.n||'分享專案'),columns,rows,colors,reportText:String(data.r||'')};return{project,locations}}
  async function shareFromHash(){const token=new URLSearchParams(location.hash.slice(1)).get('share');if(!token)return null;return expandShare(JSON.parse(await decompressShareToken(token)))}
  const today=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
  const initial=[{id:'taipei',name:'臺北',english:'Taipei',lat:25.033,lon:121.5654},{id:'como',name:'科莫',english:'Como',lat:45.8081,lon:9.0852},{id:'bellagio',name:'貝拉焦',english:'Bellagio',lat:45.9875,lon:9.2615},{id:'roma',name:'羅馬',english:'Roma',lat:41.8933,lon:12.4829},{id:'tokyo',name:'東京',english:'Tokyo',lat:35.6762,lon:139.6503}];
  function fresh(name){return{id:uid(),name,columns:initial.slice(0,4).map(c=>({id:uid(),locationId:c.id})),rows:Array.from({length:10},(_,i)=>({id:uid(),date:core.shift(today(),i)})),colors:{}}}
  const sharedNode=$('sharedProject');let shared=null;try{shared=(await shareFromHash())||(sharedNode?JSON.parse(sharedNode.textContent):null)}catch(e){document.documentElement.dataset.readonly='true';$('readonlyView').classList.add('share-error');$('readonlyTitle').textContent='分享專案無法載入';$('readonlyReport').hidden=false;$('readonlyReportText').textContent='分享連結可能不完整或已損壞，請請分享者重新產生分享網址。';return}const readonly=!!shared;
  let state;try{state=readonly?{projects:[shared.project],activeId:shared.project.id,locations:shared.locations,textSize:'large'}:JSON.parse(localStorage.getItem(KEY))}catch{}if(!state||!Array.isArray(state.projects)||!state.projects.length||!Array.isArray(state.locations)){const p=fresh('專案國外天氣');state={projects:[p],activeId:p.id,locations:initial,textSize:'large'}}
  let generation=0,controller=null,lastAttempt=0,placeTarget=null,dateTarget=null,nameMode='new',searchGeneration=0,searchController=null;const cache=new Map(),referenceCache=new Map();
  const project=()=>state.projects.find(p=>p.id===state.activeId)||state.projects[0],loc=id=>state.locations.find(l=>l.id===id),cellKey=(r,c)=>r.id+'|'+c.id;
  function sequence(p,start=p.rows[0].date){p.rows.forEach((r,i)=>r.date=core.shift(start,i))}
  state.projects.forEach(p=>sequence(p));
  function save(){if(readonly)return;try{localStorage.setItem(KEY,JSON.stringify(state))}catch{$('status').textContent='瀏覽器無法儲存，請匯出專案備份。'}}
  function el(tag,text